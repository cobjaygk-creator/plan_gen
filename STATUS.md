# plan_gen 현재 상태

## 요약
**아키텍처 전환 완료: 처음부터 슬라이드를 새로 그리는 방식 → 실제 `template.pptx`의
마커(빨강=텍스트치환, 파랑=보상콘텐츠)만 채우는 방식.** 사용자가 실제 사용 중인
template.pptx를 제공해줘서, 우측 QA표/푸터/헤더바/배경 등 템플릿의 모든 고정 요소가
손댈 필요 없이 그대로 유지됨. 카드 크기도 항목 수에 따라 유동적으로 계산됨(고정값 아님).
`run.py`가 기본으로 이 방식을 사용. 94개 테스트 통과.

- `samples/template.pptx` — 기준 템플릿 (git에 커밋됨, 핵심 의존 자산)
- 마커 탐지(안전한 raw XML 방식): `tools/template_markers.py`
- **템플릿 기반 렌더러**: `tools/render_from_template.py` — 빨강마커 텍스트치환 +
  파랑마커에 보상콘텐츠, 부족하면 슬라이드 복제(유의사항 슬라이드는 항상 마지막 유지),
  남으면 슬라이드 삭제
- 유동적 그리드 레이아웃(항목 수에 따라 카드 크기/열 자동 계산): `tools/blocks/dynamic_grid.py`
- 블록 엔진(box 파라미터 지원): `tools/blocks/` (grid/text_list/few_preview/paired_columns)
- 나머지 파이프라인(분류/매칭/파싱) 동일: `tools/pipeline.py`, `tools/classify_month.py`, `tools/match_images.py` 등

## 오늘 발견/수정한 버그 (전부 실제 202512 데이터로 검증)
1. **NEW 카드가 별도 레이아웃으로 잘못 렌더링**: new_highlight를 grid에 통합, 배지만 붙이는 방식으로 수정
2. **독립 줄 "NEW!" 마커가 이전 항목에 잘못 귀속**: 프롬프트에 "다음 항목을 가리킴" 규칙 명시
3. **이미지 매칭 탐욕 알고리즘 버그**: 최적 이분 매칭(Kuhn's algorithm)으로 교체, 6/8→7/8
4. **image_out_dir 생략 시 이미지 조용히 미삽입**: 기본값을 실제 파일 경로로 변경
5. **콘텐츠 배경 패널 누락**: 이제 template.pptx 자체가 배경을 갖고 있어 해소됨
6. **각주가 슬라이드 맨 아래 고정**: 콘텐츠 끝 바로 아래로 수정
7. **NEW 배지가 placeholder 텍스트와 겹침**: 배지 자리 확보 후 텍스트 배치
8. **`shape is marker.shape` 비교 실패**: python-pptx가 `.shapes` 순회마다 새 래퍼 객체를
   반환해서 `is` 비교가 항상 실패 — 마커 자신이 빈 텍스트로 후보에 끼어들어 치환 대상을
   밀어냄. `_element` 비교로 수정 (제목 치환이 안 되던 근본 원인)
9. **이미지 비율 무시하고 늘려서 삽입**: PIL로 실제 비율 읽어서 레터박스 방식으로 삽입하도록 수정
10. **SectionResult에 footnote 필드 누락**: 추가

## 결정된 사항
- **template.pptx가 유일한 기준 양식** — 구조/스타일 절대 재작성 안 함, 마커 영역만 조작
- 마커 규칙: 빨강(FF0000)=텍스트 치환, 파랑(00B0F0)=보상 콘텐츠, 둘 다 점선 테두리
  직사각형(채우기 없음), 텍스트 없음
- 카드 크기/열 개수는 항목 수 + 박스 크기 기반으로 유동적 계산 (고정값 아님)
- 이미지 소스는 요청서 첨부 이미지 하나뿐, 별도 에셋 저장소 없음
- 이미지는 원본 비율 유지, 카드 안에 레터박스로 삽입
- 마커 탐지는 `shape.line.color` 같은 고수준 API 대신 raw XML(`shape._element`)만 사용
  (사이드이펙트 방지 + 객체 동일성 문제 회피)

## 최근 5건
1. template.pptx 마커 기반 렌더러로 아키텍처 전환, run.py 기본 경로 변경
2. 유동적 그리드 레이아웃(dynamic_grid.py) 추가 — 항목 수 기반 카드 크기 자동 계산
3. 이미지 실제 비율 유지 삽입(PIL 레터박스), NEW 배지/카드 테두리 스타일 수정
4. `shape is marker.shape` 객체 동일성 버그 발견/수정 (텍스트 치환 안 되던 근본 원인)
5. 202512 실사용 검증 2라운드 — image_out_dir/배경패널/각주위치 버그 수정

## 다음 단계
설계서 9단계(신규 월 실사용 테스트) — 실제 다음 달 request.xlsx가 와야 의미 있게
검증됨. 그 전까지는 이번처럼 실제 산출물을 사용자가 확인하며 다듬는 라운드가 이어질 것.
