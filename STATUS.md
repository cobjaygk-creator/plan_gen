# plan_gen 현재 상태

## 요약
설계서(v2) 1~8단계 완료. 전체 파이프라인이 실제로 연결되어 10개월 샘플에 돌아감.
이미지 소스는 요청서(xlsx) 첨부 이미지 하나뿐으로 확정 — 위치 매칭 실패 항목은
텍스트만 남기고 끝. 남은 건 실제 .pptx 렌더러와 9단계(신규 월 실사용 테스트).

- 마스터 템플릿: `templates/master_template.md` (기준: 202605)
- 블록타입 분류(10개월): `templates/block_type_classification.md`
- 블록 엔진(5개 함수, A/E 통합): `tools/blocks/`
- 회귀 채점기: `tools/regression.py`
- 고정 필드 파서: `tools/parse_fixed_fields.py`
- 특별보상 원문 추출(셀+드로잉 텍스트박스+이미지유무): `tools/extract_special_reward.py`
- AI 분류기(Haiku→Sonnet 에스컬레이션): `tools/classify_month.py`, `tools/ai_client.py`
- 아이템 위치 역매칭 + 이미지 매칭(요청서 내 위치 매칭만): `tools/locate_items.py`, `tools/extract_images.py`, `tools/match_images.py`
- **전체 파이프라인 연결**: `tools/pipeline.py` (`process_month()`)
- 테스트: `tests/` — 64개 전체 통과 (`.venv/Scripts/python.exe -m pytest tests/`)

## 8단계 회귀 실행 결과 (10개월 전체, 실제 API)
9/10개월 정상 렌더링, **모든 페이지에서 겹침 0건**. 5개 블록타입 전부 실사용 데이터에서
동작 확인. 202602(설계서가 원래 버그 파일로 지목한 샘플)만 AI 응답 스키마 이상으로
NeedsHumanReview 발동 — 억지로 추측하지 않고 정상적으로 사람에게 넘김.
상세 로그: `out/regression_run_10months.txt` (gitignore 대상, 재실행으로 재생성 가능:
`tools/pipeline.py`의 `process_month()`를 월별로 호출).

이미지 매칭률은 0~94%로 월마다 편차가 큼 — 사업팀이 요청서에 이미지를 얼마나
첨부했는지에 따른 자연스러운 편차로 확정됨 (아래 참고). 매칭 안 되는 항목은
텍스트만 있는 상태로 남고, 그 이후 디자이너용 에셋 정리는 프로그램 범위 밖(수작업).

## 결정된 사항 (더 이상 미확정 아님)
- **이미지 소스는 요청서 첨부 이미지 하나뿐.** 별도 이름/ID 기반 에셋 저장소 없음 —
  확인 완료. `tools/asset_repository.py` 스텁은 제거함, `match_images()`는 위치
  매칭 전용으로 단순화.
- 이미지 화질: 원본 그대로 삽입, 카드 크기에 맞춰 리사이즈만. 별도 화질 검증/
  업스케일링 로직 만들지 않음 (디자이너 참고용이라 구분만 되면 충분).

## 최근 5건
1. 에셋 저장소 스텁 제거 — 이미지 소스가 요청서 첨부뿐이라는 게 확정되어 더 이상 필요
   없어짐. `match_images()`를 위치 매칭 전용으로 단순화, 테스트 2개 정리 (64개 통과)
2. 8단계: 파이프라인 전체 연결(`tools/pipeline.py`), 10개월 실사용 데이터로 회귀 실행.
   9/10 정상, 겹침 0건. `ai_client.py` 견고성 문제 2개 발견/수정
   (max_tokens 2048→4096, 모델이 중첩 배열을 JSON 문자열로 반환하는 문제 방어)
3. 7단계: 위치 기반 이미지 매칭 구현. 202605 종단 테스트에서 매칭률 0% 확인 →
   에셋 저장소 질문이 실제로 막히는 지점이라는 걸 실증 (이후 확인되어 해소)
4. 6단계: AI 분류기 구현, 실제 API 키로 검증. 202508 grid/text_list 오분류 발견 후
   "[이미지있음]" 마커 추가로 수정 (0.72→0.92 신뢰도 상승)
5. 5단계: 고정 필드 파서 완료, 10개월 전수 검증 통과

## 다음 단계
아직 없는 것: `Placement` 좌표 계산 결과를 실제 .pptx 파일로 써내는 렌더러
(python-pptx로 마스터 템플릿 값 적용해서 도형/이미지/텍스트를 진짜 슬라이드에
배치 → 저장). 이게 있어야 설계서 원래 목표("PPT 자동 생성")가 손에 잡힘.
그 다음이 9단계(신규 월 실사용 테스트).
