# plan_gen 현재 상태

GitHub: https://github.com/cobjaygk-creator/plan_gen (`master` 브랜치)

## 이게 뭔가
라테일 배틀패스 보상 요청서(`request.xlsx`)를 넣으면, 실제 SB 문서 양식(`samples/template.pptx`)의
마커 영역만 채워서 완성된 `.pptx`를 생성하는 파이프라인. 좌표/레이아웃 계산은 100% 코드이고,
AI는 "특별 보상 미리보기" 원문 텍스트를 구조화된 항목 목록으로 분류하는 역할만 한다.

## 사용법
```
.venv\Scripts\python.exe run.py samples/202606_request.xlsx
```
- 출력: `out/<월>/gen_<날짜>_<시분>.pptx` (예: `gen_20260807_1512.pptx`)
- 이미지: `out/<월>/images/`
- `--out`, `--images`, `--template`, `--no-template`(템플릿 없이 처음부터 그리는 구 방식) 옵션 있음
- AI 분류 단계에서 Anthropic API를 실제로 호출함 (`.env`의 `ANTHROPIC_API_KEY` 필요)

## 파이프라인 구조
```
request.xlsx
  -> parse_fixed_fields          (제목/기간/등급표/유의사항, AI 없음)
  -> extract_special_reward_rows (특별보상 구간 원문 추출, AI 없음)
  -> classify_month              (Haiku→Sonnet 에스컬레이션으로 블록타입+항목 분류)
  -> locate_items                (분류된 항목 이름을 원본 셀 좌표(행/열)로 역매칭)
  -> match_images                (같은 열 근처 이미지를 Kuhn's algorithm으로 최적 매칭)
  -> tools/blocks/*               (블록타입별 레이아웃 계산 → Placement 좌표 목록)
  -> render_from_template        (template.pptx 마커에 Placement를 실제로 그려넣음)
```

### 핵심 파일
- `samples/template.pptx` — 기준 템플릿 (git 커밋됨). 빨강 점선(FF0000)=텍스트 치환 마커,
  파랑 점선(00B0F0)=보상 콘텐츠 마커. 그 외 헤더바/우측 QA표/푸터/배경/슬라이드6 가격표는
  손대지 않음.
- `tools/template_markers.py` — 마커 탐지 (raw XML만 사용, `shape.line.color` 같은 고수준
  API는 side-effect 위험 때문에 안 씀)
- `tools/render_from_template.py` — 마커 기반 렌더러 (기본 경로)
- `tools/render_pptx.py` — 템플릿 없이 처음부터 그리는 구형 렌더러 (`--no-template`), 그리고
  `_add_placement`(모든 Placement kind 렌더링 로직)는 두 렌더러가 공유
- `tools/blocks/` — 블록 엔진 5종: `grid`, `text_list`, `few_preview`, `paired_columns`,
  `dynamic_grid`(그리드 전용 유동 페이지네이션). 항목 수/박스 크기 기반으로 카드 크기·열
  개수·페이지 분할을 전부 코드로 계산, AI는 관여 안 함
- `tools/classify_month.py` — 분류 프롬프트(`SYSTEM_PROMPT`, `PROMPT_VERSION="v6"`) +
  Haiku→Sonnet 에스컬레이션 + `ai_results/<월>.json` 캐시(프롬프트 버전이 바뀌면 자동 무효화)
- `tools/locate_items.py`, `tools/match_images.py`, `tools/extract_images.py` — 위치 기반
  이미지 매칭 (요청서에 첨부된 이미지가 유일한 소스, 별도 에셋 저장소 없음)
- `schemas/classification_schema.py`, `schemas/few_shot_examples.py` — AI 출력 스키마 +
  few-shot 예시

## 결정된 설계 원칙
- 좌표/레이아웃 = 100% 코드. AI는 텍스트 분류 + 항목 추출만.
- 이미지 소스는 요청서 첨부 이미지 하나뿐 — 별도 에셋 저장소 없음, 화질 검증/업스케일링도 안 함
  (원본 그대로 삽입 후 카드 크기에 리사이즈만)
- template.pptx가 유일한 기준 양식 — 구조/스타일 재작성 안 함, 마커 영역만 조작
- 카드 크기/열 개수/페이지 분할은 항목 수 + 실제 박스 크기 기반 유동 계산 (고정값 아님)
- 회귀 채점은 항상 숫자 기준 ("느낌"으로 합격 판정 안 함)
- 마커가 부족하면 슬라이드를 복제해서 이어 그리고, 남으면 삭제 (유의사항 슬라이드는 항상 마지막 유지)

## 지금까지 수정한 버그 (전부 실제 데이터로 검증, 시간순)

### 1라운드 — 파이프라인 최초 구축 (2025-11 ~ 2026-01경)
- 새 항목(NEW) 카드가 별도 레이아웃으로 잘못 렌더링 → grid에 통합, 배지만 붙이는 방식으로 통일
- 독립 줄 "NEW!" 마커가 바로 위 항목이 아니라 **다음** 항목을 가리킨다는 걸 프롬프트에 명시
- 이미지 매칭이 탐욕 알고리즘이라 매칭 가능한데도 놓치는 경우 발견 → Kuhn's algorithm(최적
  이분 매칭)으로 교체, 202512 기준 6/8 → 7/8
- `image_out_dir` 생략 시 이미지가 조용히 안 들어가던 버그 → 기본값을 실경로로 변경

### 2라운드 — template.pptx 마커 기반 아키텍처로 전환
- 사용자가 실제 사용 중인 template.pptx를 제공, "새로 그리기"에서 "마커만 채우기"로 전면 전환
- `shape is marker.shape` 비교가 python-pptx의 래퍼 객체 특성상 항상 실패 → `_element` 비교로
  수정 (제목 치환이 안 되던 근본 원인)
- "유의사항" 라벨이 마커 경계에 살짝 겹쳐서 잘못된 치환 대상으로 잡히던 문제 → 겹침 비율 임계값
  도입(0.4 이상만 인정)
- 이미지를 비율 무시하고 늘려서 삽입하던 문제 → PIL로 실제 비율 읽어서 레터박스 삽입으로 수정
- 콘텐츠가 페이지를 넘칠 때 레이아웃 기준 박스를 첫 번째 슬롯이 아니라 **가장 작은** 슬롯으로
  잡도록 수정 (마지막 페이지가 항상 가장 작은 유의사항 슬라이드에 랜딩하기 때문)

### 3라운드 — 이중 중첩 JSON 스키마 버그 (202602/202603)
- Anthropic API가 가끔 `{"sections": "<sections를 담은 JSON 문자열>"}` 형태로 응답 → 기존
  언스트링화 로직을 통과해도 `{"sections": {"sections": [...]}}`로 한 번 더 중첩된 채 남아
  검증 실패. 원시 API 응답을 직접 캡처해서 원인 확정 후 `_unwrap_self_nesting()` 추가로 해결

### 4라운드 — paired_columns 구조적 재설계 (202606 "배틀패스 신규 의상" 실사용 문제, 총 3커밋)
사용자가 요청서 원본·사람이 만든 참고 PPT·생성 결과물을 스크린샷으로 비교하며 발견한 문제들.

**(1) 세트별 하위 목록 구조 유실** — "자켓 세트"/"치마 세트" 두 세트가 각각 자기만의 하위
구성품 목록을 가진 구조인데, 하나의 flat 그리드 15개 항목으로 잘못 분류되고 있었음.
`Item`에 `pair_group`(0/1/null) 필드 추가, 분류 프롬프트+few-shot 갱신, `paired_columns_block`을
`sub_items_by_pair`(세트별 개별 목록) 방식으로 재작성. `SectionResult`가 원본 `items`를 직접
보관하도록 변경(렌더링된 Placement에서 역추적하던 방식은 하위 항목처럼 dict가 아닌 값을 담는
placement에서 정보가 유실됨).

**(2) 실사용 렌더링 버그 4건** (스크린샷 재검토로 발견):
- 매칭된 하위 항목 이미지가 그려지지 않음 (당시엔 텍스트 전용 Placement만 생성했음)
- 각주 두 번째 줄이 거대한 폰트로 유의사항 영역까지 침범 — `\n` 포함 문자열을 `text_frame.text`에
  통째로 넣으면 python-pptx가 자동으로 문단을 나누는데, 첫 문단에만 폰트 스타일을 줬던 게 원인.
  모든 문단에 명시적으로 스타일 적용하도록 수정
- AI가 "분홍 장미꽃"/"초롱 눈빛"처럼 의도적으로 반복된 동일 이름 행을 중복으로 오판해 하나로
  합쳐버림 (실제로는 착용 파츠가 다른 별개 항목 2개) → 프롬프트에 규칙 추가 + 실제 데이터 기반
  few-shot 예시로 교정, `PROMPT_VERSION` v5로 캐시 무효화
- 세트를 감싸는 카드 프레임이 이미지에만 걸리고 하위 목록은 테두리 밖에 떠 있던 문제 → 콘텐츠
  실제 높이에 맞춰 동적으로 커지는 카드로 재구현 (`Placement` kind `"frame"` 신설)

**(3) 이미지 매칭 열 불일치** — "자켓 세트" 텍스트는 B열, 포트레이트 이미지는 C열에 앵커되어
있어 열 완전일치 조건 때문에 아예 매칭 후보가 0개였음. 단순히 열 허용범위만 넓히면 하위 항목의
행이 이미지 앵커 범위 안에 정확히 들어가서(거리=0) 세트 이름(거리>0)보다 먼저 이미지를 가로채는
역효과가 있었음 → `paired_columns` 섹션에서는 세트 이름(`pair_group=None`) 2개만 매칭 대상으로
삼고 하위 항목은 매칭 자체를 시도하지 않도록 분리, 그 좁혀진 후보군에 한해 열 허용범위 확대.
아이콘 UI는 사용자가 편집한 선호 버전 + 원본 참고 PPT 둘 다 텍스트 전용이라 되돌림.

### 5라운드 — paired_columns 분류 규칙 일반화 + 구조적 검증 안전장치 (202503 실사용 문제)
202606과 다른 형태의 파일(202503 "버니버니 의상 선택권")을 돌려보니, 최상위 세트 이름 2개가
슬래시 병기가 아니라 다른 하위 구성품 행들과 **똑같이 생긴 2열 행**으로 적혀 있어서 AI가 구분
신호를 못 찾고 세트 이름까지 하위 구성품으로 태깅 → `pair_items`가 0개가 되어 그 섹션 전체가
결과물에서 조용히 통째로 빠짐(콘솔 로그 외엔 티가 안 남).

사용자 지적: "이게 정해진 걸 내놓는 게 아니라 기존 데이터를 학습해서 향후 비슷한 유형도 자동
처리하기 위함인데, 형태가 다르다고 에러 나는 게 정상인가" — 맞는 지적. 원인은 AI의 한계가
아니라 프롬프트 규칙이 202606의 **표면적 형태(슬래시 병기)** 에만 좁게 쓰여 있었던 것.

수정 2가지:
1. `classify_month.py`의 paired_columns 규칙을 표면 형태가 아니라 일반 원칙으로 재작성 —
   "'···중 택 1' 각주가 가리키는 명사와 이름이 일치하는 항목이 최상위 선택지 2개, 나머지는
   그 소속에 따라 pair_group을 매긴다"는 식으로, 형태에 안 얽매이게. 202503 사례(세트명이
   2열 행 형식)를 few-shot 예시로 추가. `PROMPT_VERSION` v6.
2. **구조적 검증 안전장치** — `paired_columns` 섹션인데 `pair_group=null` 항목이 정확히
   2개가 아니면, AI가 스스로 보고한 confidence와 무관하게 "신뢰 불가"로 처리해서
   Sonnet 재검토로 에스컬레이션하고, 그래도 안 되면 `NeedsHumanReview`로 사람이 확인하게
   막음(파일 자체를 생성 안 함). 이전엔 이런 구조적 실패가 render 단계에서 `ValueError`로만
   잡혀서 그 섹션만 조용히 빠지고 나머지는 정상 생성됐었음 — 이제는 "콘텐츠가 빠진 채
   조용히 완성된 것처럼 보이는" 상황 자체를 차단.

### 기타
- 결과 파일명을 `gen_<날짜>_<시분>.pptx`로 변경 (기존 고정 `generated.pptx`에서)
- `SectionResult`에 `footnote`, `items` 필드 추가

## 테스트
114개 통과 (`pytest tests/`). 실제 202503/202512/202605/202606 요청서를 활용한 회귀 테스트
다수 포함.

## 다음 단계
설계서 9단계(신규 월 실사용 테스트) — 다음 달 실제 request.xlsx가 오면 이번처럼 실제 산출물을
사용자가 확인하며 다듬는 라운드가 이어질 예정.
