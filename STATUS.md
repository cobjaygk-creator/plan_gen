# plan_gen 현재 상태

GitHub: https://github.com/cobjaygk-creator/plan_gen (`master` 브랜치)

## 이게 뭔가
라테일 배틀패스 보상 요청서(`request.xlsx`)를 넣으면, 실제 SB 문서 양식(`samples/template.pptx`)의
마커 영역만 채워서 완성된 `.pptx`를 생성하는 파이프라인. 좌표/레이아웃 계산은 100% 코드이고,
AI는 "특별 보상 미리보기" 원문 텍스트를 구조화된 항목 목록으로 분류하는 역할만 한다.

이 CLI 파이프라인(`tools/`, `run.py`) 위에 웹 UI(`web/`)도 있음 — 본인 PC에서 로그인해서
브라우저로 업로드/생성/이력조회 하는 용도로 완성돼 있음 (팀 배포는 아직 불필요, 필요시 나중에).
실행법은 `web/backend/README.md` 참고 (요약: `web/run_local.ps1` 한 줄).

## 사용법
```
.venv\Scripts\python.exe run.py samples/202606_request.xlsx
```
- 출력: `out/<월>/gen_<날짜>_<시분>.pptx` (예: `gen_20260807_1512.pptx`)
- 이미지: `out/<월>/images/`
- `--out`, `--images`, `--template`, `--no-template`(템플릿 없이 처음부터 그리는 구 방식) 옵션 있음
- AI 분류 단계에서 Anthropic API를 실제로 호출함 (`.env`의 `ANTHROPIC_API_KEY` 필요)

## 파이프라인 구조
```
request.xlsx
  -> parse_fixed_fields          (제목/기간/등급표/유의사항, AI 없음)
  -> extract_special_reward_rows (특별보상 구간 원문 추출, AI 없음)
  -> classify_month              (Haiku→Sonnet 에스컬레이션으로 블록타입+항목 분류)
  -> locate_items                (분류된 항목 이름을 원본 셀 좌표(행/열)로 역매칭)
  -> match_images                (같은 열 근처 이미지를 Kuhn's algorithm으로 최적 매칭)
  -> tools/blocks/*               (블록타입별 레이아웃 계산 → Placement 좌표 목록)
  -> render_from_template        (template.pptx 마커에 Placement를 실제로 그려넣음)
```

### 핵심 파일
- `samples/template.pptx` — 기준 템플릿 (git 커밋됨). 빨강 점선(FF0000)=텍스트 치환 마커,
  파랑 점선(00B0F0)=보상 콘텐츠 마커. 그 외 헤더바/우측 QA표/푸터/배경/슬라이드6 가격표는
  손대지 않음.
- `tools/template_markers.py` — 마커 탐지 (raw XML만 사용, `shape.line.color` 같은 고수준
  API는 side-effect 위험 때문에 안 씀)
- `tools/render_from_template.py` — 마커 기반 렌더러 (기본 경로)
- `tools/render_pptx.py` — 템플릿 없이 처음부터 그리는 구형 렌더러 (`--no-template`), 그리고
  `_add_placement`(모든 Placement kind 렌더링 로직)는 두 렌더러가 공유
- `tools/blocks/` — 블록 엔진 6종: `grid`, `text_list`, `few_preview`, `paired_columns`,
  `icon_only`, `dynamic_grid`(그리드 전용 유동 페이지네이션). 항목 수/박스 크기 기반으로
  카드 크기·열 개수·페이지 분할을 전부 코드로 계산, AI는 관여 안 함
- `tools/classify_month.py` — 분류 프롬프트(`SYSTEM_PROMPT`, `PROMPT_VERSION="v7"`) +
  Haiku→Sonnet 에스컬레이션 + `ai_results/<월>.json` 캐시(프롬프트 버전이 바뀌면 자동 무효화)
- `tools/locate_items.py`, `tools/match_images.py`, `tools/extract_images.py` — 위치 기반
  이미지 매칭 (요청서에 첨부된 이미지가 유일한 소스, 별도 에셋 저장소 없음)
- `schemas/classification_schema.py`, `schemas/few_shot_examples.py` — AI 출력 스키마 +
  few-shot 예시

## 결정된 설계 원칙
- 좌표/레이아웃 = 100% 코드. AI는 텍스트 분류 + 항목 추출만.
- 이미지 소스는 요청서 첨부 이미지 하나뿐 — 별도 에셋 저장소 없음, 화질 검증/업스케일링도 안 함
  (원본 그대로 삽입 후 카드 크기에 리사이즈만)
- template.pptx가 유일한 기준 양식 — 구조/스타일 재작성 안 함, 마커 영역만 조작
- 카드 크기/열 개수/페이지 분할은 항목 수 + 실제 박스 크기 기반 유동 계산 (고정값 아님)
- 회귀 채점은 항상 숫자 기준 ("느낌"으로 합격 판정 안 함)
- 마커가 부족하면 슬라이드를 복제해서 이어 그리고, 남으면 삭제 (유의사항 슬라이드는 항상 마지막 유지)

## 지금까지 수정한 버그 (전부 실제 데이터로 검증, 시간순)

### 1라운드 — 파이프라인 최초 구축 (2025-11 ~ 2026-01경)
- 새 항목(NEW) 카드가 별도 레이아웃으로 잘못 렌더링 → grid에 통합, 배지만 붙이는 방식으로 통일
- 독립 줄 "NEW!" 마커가 바로 위 항목이 아니라 **다음** 항목을 가리킨다는 걸 프롬프트에 명시
- 이미지 매칭이 탐욕 알고리즘이라 매칭 가능한데도 놓치는 경우 발견 → Kuhn's algorithm(최적
  이분 매칭)으로 교체, 202512 기준 6/8 → 7/8
- `image_out_dir` 생략 시 이미지가 조용히 안 들어가던 버그 → 기본값을 실경로로 변경

### 2라운드 — template.pptx 마커 기반 아키텍처로 전환
- 사용자가 실제 사용 중인 template.pptx를 제공, "새로 그리기"에서 "마커만 채우기"로 전면 전환
- `shape is marker.shape` 비교가 python-pptx의 래퍼 객체 특성상 항상 실패 → `_element` 비교로
  수정 (제목 치환이 안 되던 근본 원인)
- "유의사항" 라벨이 마커 경계에 살짝 겹쳐서 잘못된 치환 대상으로 잡히던 문제 → 겹침 비율 임계값
  도입(0.4 이상만 인정)
- 이미지를 비율 무시하고 늘려서 삽입하던 문제 → PIL로 실제 비율 읽어서 레터박스 삽입으로 수정
- 콘텐츠가 페이지를 넘칠 때 레이아웃 기준 박스를 첫 번째 슬롯이 아니라 **가장 작은** 슬롯으로
  잡도록 수정 (마지막 페이지가 항상 가장 작은 유의사항 슬라이드에 랜딩하기 때문)

### 3라운드 — 이중 중첩 JSON 스키마 버그 (202602/202603)
- Anthropic API가 가끔 `{"sections": "<sections를 담은 JSON 문자열>"}` 형태로 응답 → 기존
  언스트링화 로직을 통과해도 `{"sections": {"sections": [...]}}`로 한 번 더 중첩된 채 남아
  검증 실패. 원시 API 응답을 직접 캡처해서 원인 확정 후 `_unwrap_self_nesting()` 추가로 해결

### 4라운드 — paired_columns 구조적 재설계 (202606 "배틀패스 신규 의상" 실사용 문제, 총 3커밋)
사용자가 요청서 원본·사람이 만든 참고 PPT·생성 결과물을 스크린샷으로 비교하며 발견한 문제들.

**(1) 세트별 하위 목록 구조 유실** — "자켓 세트"/"치마 세트" 두 세트가 각각 자기만의 하위
구성품 목록을 가진 구조인데, 하나의 flat 그리드 15개 항목으로 잘못 분류되고 있었음.
`Item`에 `pair_group`(0/1/null) 필드 추가, 분류 프롬프트+few-shot 갱신, `paired_columns_block`을
`sub_items_by_pair`(세트별 개별 목록) 방식으로 재작성. `SectionResult`가 원본 `items`를 직접
보관하도록 변경(렌더링된 Placement에서 역추적하던 방식은 하위 항목처럼 dict가 아닌 값을 담는
placement에서 정보가 유실됨).

**(2) 실사용 렌더링 버그 4건** (스크린샷 재검토로 발견):
- 매칭된 하위 항목 이미지가 그려지지 않음 (당시엔 텍스트 전용 Placement만 생성했음)
- 각주 두 번째 줄이 거대한 폰트로 유의사항 영역까지 침범 — `\n` 포함 문자열을 `text_frame.text`에
  통째로 넣으면 python-pptx가 자동으로 문단을 나누는데, 첫 문단에만 폰트 스타일을 줬던 게 원인.
  모든 문단에 명시적으로 스타일 적용하도록 수정
- AI가 "분홍 장미꽃"/"초롱 눈빛"처럼 의도적으로 반복된 동일 이름 행을 중복으로 오판해 하나로
  합쳐버림 (실제로는 착용 파츠가 다른 별개 항목 2개) → 프롬프트에 규칙 추가 + 실제 데이터 기반
  few-shot 예시로 교정, `PROMPT_VERSION` v5로 캐시 무효화
- 세트를 감싸는 카드 프레임이 이미지에만 걸리고 하위 목록은 테두리 밖에 떠 있던 문제 → 콘텐츠
  실제 높이에 맞춰 동적으로 커지는 카드로 재구현 (`Placement` kind `"frame"` 신설)

**(3) 이미지 매칭 열 불일치** — "자켓 세트" 텍스트는 B열, 포트레이트 이미지는 C열에 앵커되어
있어 열 완전일치 조건 때문에 아예 매칭 후보가 0개였음. 단순히 열 허용범위만 넓히면 하위 항목의
행이 이미지 앵커 범위 안에 정확히 들어가서(거리=0) 세트 이름(거리>0)보다 먼저 이미지를 가로채는
역효과가 있었음 → `paired_columns` 섹션에서는 세트 이름(`pair_group=None`) 2개만 매칭 대상으로
삼고 하위 항목은 매칭 자체를 시도하지 않도록 분리, 그 좁혀진 후보군에 한해 열 허용범위 확대.
아이콘 UI는 사용자가 편집한 선호 버전 + 원본 참고 PPT 둘 다 텍스트 전용이라 되돌림.

### 5라운드 — paired_columns 분류 규칙 일반화 + 구조적 검증 안전장치 (202503 실사용 문제)
202606과 다른 형태의 파일(202503 "버니버니 의상 선택권")을 돌려보니, 최상위 세트 이름 2개가
슬래시 병기가 아니라 다른 하위 구성품 행들과 **똑같이 생긴 2열 행**으로 적혀 있어서 AI가 구분
신호를 못 찾고 세트 이름까지 하위 구성품으로 태깅 → `pair_items`가 0개가 되어 그 섹션 전체가
결과물에서 조용히 통째로 빠짐(콘솔 로그 외엔 티가 안 남).

사용자 지적: "이게 정해진 걸 내놓는 게 아니라 기존 데이터를 학습해서 향후 비슷한 유형도 자동
처리하기 위함인데, 형태가 다르다고 에러 나는 게 정상인가" — 맞는 지적. 원인은 AI의 한계가
아니라 프롬프트 규칙이 202606의 **표면적 형태(슬래시 병기)** 에만 좁게 쓰여 있었던 것.

수정 2가지:
1. `classify_month.py`의 paired_columns 규칙을 표면 형태가 아니라 일반 원칙으로 재작성 —
   "'···중 택 1' 각주가 가리키는 명사와 이름이 일치하는 항목이 최상위 선택지 2개, 나머지는
   그 소속에 따라 pair_group을 매긴다"는 식으로, 형태에 안 얽매이게. 202503 사례(세트명이
   2열 행 형식)를 few-shot 예시로 추가. `PROMPT_VERSION` v6.
2. **구조적 검증 안전장치** — `paired_columns` 섹션인데 `pair_group=null` 항목이 정확히
   2개가 아니면, AI가 스스로 보고한 confidence와 무관하게 "신뢰 불가"로 처리해서
   Sonnet 재검토로 에스컬레이션하고, 그래도 안 되면 `NeedsHumanReview`로 사람이 확인하게
   막음(파일 자체를 생성 안 함). 이전엔 이런 구조적 실패가 render 단계에서 `ValueError`로만
   잡혀서 그 섹션만 조용히 빠지고 나머지는 정상 생성됐었음 — 이제는 "콘텐츠가 빠진 채
   조용히 완성된 것처럼 보이는" 상황 자체를 차단.

### 6라운드 — 이미지 전용(icon_only) 블록타입 신설 (202602 "고양이 모자 선택권")
사용자가 실제 결과물과 사람이 만든 참고 PPT를 비교하다가, "AI가 확신 있게 분류 못해서
사람 검토가 필요합니다"라는 메시지가 뜬 이유를 물어봐서 원본을 직접 열어봄. 두 섹션이
걸렸는데 그중 하나("[이벤트] 고양이 모자 선택권")는 원인이 명확했음: 원본 엑셀에 제목+각주만
있고 그 아래 항목 이름이 하나도 없이 이미지(9개 아이콘이 합쳐진 그림 1장) 하나만 붙어있었음.

**근본 원인**: `extract_special_reward_rows()`가 셀에 텍스트가 하나도 없는 행은 이미지가
붙어있어도 통째로 걸러내고 있었음 — 그래서 AI 분류기는 그 이미지의 존재 자체를 몰랐음
(텍스트만 보고 판단하는 구조라 프롬프트를 아무리 고쳐도 원천적으로 해결 불가능한 문제였음).
`templates/block_type_classification.md`(초기 10개월 전수조사 기록)를 다시 확인해보니 이
패턴이 202506에서도 비슷하게 관측돼있어서, 한 달짜리 예외가 아니라 일반화할 가치가 있는
반복 패턴으로 판단.

(참고로 같은 파일에서 발견된 또 다른 애매한 케이스 — "배틀패스 의상 교환권" 19개 목록에
제목 없이 붙어있던 "버니버니 세트 I/II (new!)" 서브블록 — 는 애초에 프로젝트 초기 조사
때부터 "표가 2개인 이상 파일"로 별도 표시돼 있던 케이스라, 스키마를 확장해서까지
대응할 실익이 없다고 판단해 보류함. 결국 AI가 이걸 별도의 작은 new_highlight 섹션으로
분리하는 합리적인 해석을 스스로 찾아냈음.)

수정:
1. `extract_special_reward_rows()`: 셀 텍스트가 없어도 이미지가 앵커된 행은 이제 포함 —
   `to_compact_text()`가 이런 행을 "42: [이미지있음]"처럼 이름 없는 마커 줄로 표시.
2. `BlockType`에 `icon_only` 추가, 분류 프롬프트에 패턴 설명 + 실제 202602 데이터 기반
   few-shot 추가, `PROMPT_VERSION` v7.
3. `tools/blocks/icon_only.py` 신설 — 캡션 없이 이미지만 박스에 맞춰 배치 (dynamic_grid와
   같은 box-fitting 방식, 페이지네이션 지원).
4. `pipeline.py`: `icon_only` 섹션은 AI 항목이름→위치찾기→이미지매칭이라는 기존 흐름을
   완전히 건너뛰고, 섹션 제목 행부터 다음 섹션 전까지의 행 범위를 직접 스캔해서 그 안에
   앵커된 이미지를 바로 가져옴 (이름이 아예 없으니 이름 기반 매칭이 원천적으로 불가능).

실제 202602로 검증: AI가 `icon_only`로 정확히 분류(items=[]), 파이프라인이 실제 고양이 모자
이미지 1장을 찾아서 정상 렌더링(`img=100%`) — 이전엔 이 섹션 때문에 `NeedsHumanReview`로
막혀서 파일 자체가 생성 안 됐었음.

### 7라운드 — paired_columns 오탐 방지: 각주 문구가 아니라 열 구조로 판단 (실사용 스크린샷 신고)
사용자가 다른 달 요청서(샘플 폴더엔 없는 파일)를 웹 UI로 직접 생성해보고 스크린샷 3장을
보내옴: 완성된 옷 세트 14개가 전부 대등하게 나열돼야 하는데(참고 이미지), 실제로는 그중
2개만 골라 최상위로 잡고 나머지 12개를 억지로 하위 구성품처럼 두 그룹으로 나눠버림.

**원인 추정** (해당 파일을 직접 못 구해서 스크린샷 기반 추론): 이 섹션의 각주("아래 패션
세트 중 택 1")가 202503 few-shot 예시의 각주 문구와 완전히 동일 — 프롬프트가 "택1 각주 +
비슷한 이름 패턴"에 과적합해서, 각 항목이 진짜 부위 조각인지 확인 안 하고 무리하게
2그룹으로 나눈 것으로 추정. 실제로 202602의 거의 동일한 구조("배틀패스 의상 교환권",
19개 완성 세트 + "택1" 각주)는 처음부터 grid로 정상 분류돼 있었는데, 그 차이를 만드는
진짜 신호는 각주 문구가 아니라 **하위 항목이 2열로 정확히 짝지어진 행에서 나왔는지**였음
(202503/202606은 한 행에 "세트0 몫 | 세트1 몫" 정확히 2개, 202602는 한 행에 서로 무관한
항목 3개 이상 — 짝이 아니라 그냥 나열).

**수정**: 프롬프트에 "각주 문구만으로 판단하지 말고, 하위 항목이 정확히 2열 짝행에서 나온
경우에만 paired_columns, 3열 이상 flowing 목록이면 각주가 비슷해도 grid"라는 구조적 규칙
명시. 202602의 실제(이미 정상이었던) 데이터를 "이건 왜 paired_columns가 아닌지" 보여주는
대조 few-shot으로 추가. `PROMPT_VERSION` v8.

**검증**: 원인이 된 실제 파일은 확보 못해서 직접 재현 검증은 못했지만, 실제 API로 3개
회귀 케이스를 재확인 — 202602(오탐 유발 패턴과 구조가 같은 "배틀패스 의상 교환권")는
여전히 정상적으로 grid 유지, 202503·202606(진짜 paired_columns여야 하는 케이스) 둘 다
여전히 정상적으로 paired_columns 유지. 새 규칙이 진짜 양성 케이스를 깨뜨리지 않으면서
오탐 유형은 구조적으로 걸러내도록 확인됨.

### 8라운드 — 같은 열에 겹친 텍스트박스 캡션이 조용히 사라짐 (실사용 파일+스크린샷 신고, 202508)
사용자가 이번엔 실제 파일(`samples/202508_request.xlsx`, 결과는 `samples/202508_result.pptx`)과
스크린샷 3장을 함께 제공 — "배틀패스 의상 교환권"(요청서 기준 21개 완성 세트)이 이름표도
없이 4칸짜리 `icon_only` 그리드로 나왔고, 그중 한 칸은 세트 이미지가 아니라 섹션 제목
옆의 "WEAR" 교환권 뱃지 아이콘 자체였음.

**근본 원인** (실제 파일로 재현·확인, 추정 아님): `extract_special_reward.py`가 드로잉
XML의 플로팅 텍스트박스를 `col_idx0`(정수 열 인덱스)만으로 스프레드시트 열 문자에
매핑하는데, 실제 요청서는 한 시각적 행에 4개 항목이 들어가면서 열 2개 폭 안에 텍스트박스
4개가 `colOff`(같은 열 안에서의 좌우 오프셋)만 다르게 겹쳐 들어있었음 (실측: "화이트 니트
세트"와 "브라운 니트 세트" 둘 다 `col_idx0=2`, colOff는 539213 vs 2035635로 전혀 다름).
병합 코드가 `dict.setdefault(col_letter, text)`였어서 같은 열 문자로 매핑된 두 번째
텍스트박스는 조용히 버려짐 — 21개 중 16개만 AI에게 전달됐고, 그마저 앞부분엔 이름 없는
이미지 마커 행이 잔뜩 이어져서 AI가 전체를 `icon_only`(items=0)로 오분류함. `icon_only`
경로는 이름 매칭을 아예 건너뛰고 섹션 제목~다음 섹션 사이 행 범위의 이미지를 좌표로만
쓸어담기 때문에, 제목 바로 옆에 앵커된 WEAR 뱃지까지 "항목 이미지"로 딸려 들어가고 범위
스캔은 첫 실제 텍스트 행에서 멈춰버려 4장만 남은 것.

**수정**: `extract_drawing_text_anchors()`가 이제 `colOff`도 같이 반환. 같은 행 안에서 여러
텍스트박스가 같은 `col_letter`로 충돌하면, 왼쪽→오른쪽 실제 위치 순서로 정렬한 뒤 비어있는
가장 가까운 다른 열 문자에 배정 — 버리지 않음. 부수적으로 `&amp;` 같은 XML 엔티티가
캡션에 그대로 남아있던 것도 `html.unescape()`로 같이 고침("빌브라트 수트 &amp; 펀치 mini"
→ "빌브라트 수트 & 펀치 mini").

**검증**: 수정 전/후로 `samples/*_request.xlsx` 전체 원문 추출을 diff — 이 충돌 패턴이
202508 말고도 다른 몇 개월 치 몇몇 섹션에서 항목을 조용히 빠뜨리고 있었던 것도 함께 발견
(항목이 새로 나타나기만 하고 기존 항목이 사라지거나 바뀐 곳은 없음, 안전한 개선). 202508을
실제 파이프라인으로 재실행 — AI가 이제 정확히 `grid`, 21개 항목으로 분류(재분류 전 5개
이름만 보였던 것과 달리, 프롬프트는 안 건드림에도 원문이 고쳐지자 스스로 정상 분류함),
렌더링된 실제 PPT를 PowerPoint로 열어 육안 확인까지 완료 — `기대한 페이지` 스크린샷과 이름·
이미지·헤더 스타일 전부 일치. 이미지 매칭률은 81%(21개 중 17개, 겹침 없음)로 여전히
100%는 아님 — 그림 앵커 쪽도 같은 `col_idx0`-only 한계가 있어서 미매칭 항목 일부가 남는데,
이건 "이미지 없이 텍스트만"이라는 기존에도 유효한 정상 상태라 이번 라운드에서는 손대지
않음(추가 신고 시 별도 처리).

**추가 안전장치 (구조적, 비용 없음)**: `icon_only` 경로가 이 사고를 실제로 일으킨 지점은
아니었지만(원인은 텍스트 추출 단계), 혹시 다른 이유로 어떤 섹션이 앞으로 다시 `icon_only`로
오분류되더라도 같은 증상(제목 옆 뱃지가 항목으로 딸려 들어감)이 재발하지 않도록
`tools/pipeline.py`의 `_icon_only_items()`에 구조적 규칙을 하나 추가함: 이미지 앵커의
`row_start`가 섹션 제목 행 이하이면(= 제목 자체에 붙어있는 장식 아이콘) 애초에 이미지
쓸어담기 후보에서 제외. 실제 데이터로 검증: 202508의 WEAR 뱃지(`row_start=17`, 제목도
`row=17`)는 제외되고, 202602의 진짜 아이콘 9장(`row_start=42`, 제목은 `row=40`)은 여전히
정상 포함 — `tests/test_pipeline.py`에 두 케이스 모두 회귀 테스트로 추가.

**검토했지만 보류: 이미지 내용을 보는 AI 비전 검증 단계**. 사용자가 이 사고를 스크린샷
2장만 보고 "이미지 매칭이 이미지 내용을 안 봐서 장식 아이콘과 실제 항목 이미지를 구분
못 한다"는 가설로 진단하고, `match_images` 뒤에 멀티모달 판정 단계를, 렌더링 후에는
슬라이드 전체를 다시 비전 모델로 자기 점검하는 단계를 추가하자는 상세 작업지시서를 제안함.
실제 파일로 재현해보니 진단이 달랐음 — `match_images`는 이 섹션에 대해 아예 호출된 적이
없었고(위 "근본 원인" 참고, `icon_only` 경로는 이름 매칭 자체를 건너뜀), 실제 원인은 텍스트
추출 단계의 결정론적 버그였음. 사용자도 이 반박에 동의하고 방향을 다음과 같이 확정함:

- 이번 사고는 이미 훨씬 싼 구조적 수정으로 해결·검증됨 — 비전 검증이 있었어도 애초에
  이 사고를 잡지 못했을 경로였으므로, "이번 문제 대응"으로는 무의미함.
- 비전 검증은 "이미지 내용을 실제로 봐야만 구분되는 케이스"가 실제로 발생해야 정당화되는
  선제 투자인데, 그런 케이스가 지금까지 한 번도 실증된 적이 없음(지금까지의 모든 실사용
  버그는 전부 위치·구조 기반 결정론적 신호로 설명·수정됨).
- 이 프로젝트가 지금까지 일관되게 지켜온 "위치 기반만, AI는 텍스트 분류처럼 꼭 필요한
  곳에만" 원칙(design principle 3, `match_images.py` docstring)과도 맞지 않음.
- 매 생성마다 추가 API 비용·지연시간이 드는 상시 비용이라, 근거 없이 들이기엔 이르다.

**→ 결론: 비전 검증 단계는 지금 만들지 않는다.** 나중에 정말로 "위치·구조 규칙만으로는
설명이 안 되고, 이미지 내용을 직접 봐야만 구분되는" 실제 사례가 새로 나오면 그때 이 절을
참고해서 재검토할 것 — 그 전까지는 이 판단을 뒤집을 근거가 없다는 뜻으로 읽을 것.

### 9라운드 — 이미지 없는 항목이 빈 카드로 나오던 것을 텍스트 줄로 (사용자 데모 비교)
사용자가 (이 세션 이전에) 다른 Claude 대화에서 202508 요청서를 직접 열어보고 만든 참고
데모 스크린샷 3장을 제시 — 보상 항목이 하나도 안 빠지고, 이미지 있는 건 이미지+캡션으로,
없는 건 깔끔한 텍스트 박스로 나오는 걸 "원하는 방향"으로 확인. 실제 파이프라인은 이미지
없는 항목도 그냥 빈 이미지 카드(흰 박스)로 렌더링하고 있었음(202508의 "산뜻한 소망 세트"
등 4개, "배틀패스 대미지 스킨 교환권"의 8개).

**주의**: 실제 프로덕션 렌더러는 `tools/pipeline.py`의 `_render_section`(→`grid_block`)이
아니라 `tools/render_from_template.py`의 `_render_section_pages`(→
`tools/blocks/dynamic_grid.py`의 `fit_grid_pages`)다 — 웹서비스(`pipeline_runner.py`)와
`run.py` 둘 다 이쪽을 씀. 처음에 `grid_block`만 고치고 렌더링해봤더니 화면이 그대로였던
게 이 착오 때문 — 두 렌더러 다 고쳐야 함을 뒤늦게 확인.

**수정**: `tools/blocks/grid.py`에 `grid_with_text_overflow_block`, `tools/blocks/dynamic_grid.py`에
`fit_grid_pages_with_text_overflow`를 각각 추가(기존 `grid_block`/`fit_grid_pages`는 안 건드림,
새 함수가 내부적으로 재사용). 이미지 있는 항목만 기존 그리드 로직대로 배치하고, 그 아래
남는 세로 공간에 이미지 없는 항목을 텍스트 줄로 채움(다 못 채우면 다음 페이지로 이월,
text_list_block과 같은 방식). `render_from_template.py`의 실제 호출부를
`fit_grid_pages_with_text_overflow`로 교체.

**부수 효과**: 섹션에 이미지 있는 항목이 하나도 없으면 결과가 `text_list_block`과 똑같아짐
— AI가 이 섹션을 `grid`로 분류하든 `text_list`로 분류하든 화면은 같아지므로, 지난 라운드에
관찰됐던 분류 흔들림(실행마다 둘 사이를 오가던 것)이 더 이상 화면에 영향을 주지 않음.

**검증**: 새 유닛 테스트 13개(`tests/test_blocks.py`, `tests/test_dynamic_grid.py`) — 혼합
케이스/전부 이미지 있음(기존 함수와 동일 결과인지)/전부 이미지 없음(text_list처럼 보이는지)/
빈 목록/NEW 배지 없이 텍스트 접미사만 붙는지 등. 202508을 실제로 다시 렌더링해서 PowerPoint로
직접 열어 확인 — 17개 이미지 있는 항목은 한 페이지에 깔끔히, 나머지 4개(산뜻한 소망 세트 등)는
빈 박스 없이 다음 페이지에 텍스트 줄로 정상 출력. 항목 손실 없음(9개 전부 나옴, 이미지 스킨
섹션도 1개 이미지+8개 텍스트로 전부 확인). 전체 143개 테스트 통과.

**다듬음 (같은 라운드 후속)**: 위에서 남겨둔 "이미지 1개뿐인 페이지에서 텍스트가 1개만
들어가던" 문제의 진짜 원인을 찾아 고침 — 텍스트 줄을 배치할 때 열 개수를 이미지 그리드가
쓴 열 개수(`_page_columns`로 유추)를 그대로 재사용하고 있었는데, 이미지가 1개뿐이면
`fit_grid_pages`가 아이콘을 최대한 키우려고 1열만 씀 — 그 1열을 텍스트 줄에도 그대로
써서 박스 너비 3분의 2를 그냥 버리고 있었음. 텍스트 줄은 이미지 그리드와 무관하게 항상
`TEXT_COLUMNS=3`을 쓰도록 고침. 실측(202508 대미지 스킨 섹션, 실제 박스 460×293pt):
이미지 1개(200pt로 최대 확대) 밑에 남는 공간이 텍스트 1줄(3칸)만큼은 됐는데, 이전엔 그
1줄을 1칸으로만 썼던 것 — 고친 뒤엔 같은 공간에 3개가 들어가고 남은 5개만 다음 페이지로.
실제 재렌더링으로 확인, 143개 테스트 여전히 통과.

**시도했다가 되돌린 것 — 이미지 앵커 충돌 "수정"**: 텍스트 캡션과 같은 원리로 사진도
`col_idx0`만으로 열을 판단해서 여러 장이 한 열에 겹치는 걸 발견하고 처음엔 같은 방식(colOff
기준 재배정)으로 고치려 했음. 그런데 실제 202512 회귀 테스트(`tests/test_regression_202512.py`,
과거에 실제 파일로 손검증된 정답)가 깨짐 — 원인을 이미지 파일로 직접 열어서 확인해보니,
"충돌"로 보였던 두 이미지 중 하나는 진짜 헤어스타일 미리보기 사진이고 다른 하나는 작은
장식 뱃지 아이콘이었음(이번 사고의 WEAR 뱃지와 같은 패턴). 기존 코드는 이 충돌 덕분에
(의도한 설계는 아니지만) 뱃지 쪽이 매칭에서 밀려나 있었는데, 내가 "충돌은 항상 버그"라고
가정하고 둘 다 매칭되게 고치면 뱃지가 진짜 콘텐츠 이미지처럼 승격되는, 이번 사고와 같은
종류의 실수를 새로 만드는 것이었음. **이미지 충돌은 텍스트 캡션 충돌과 성격이 다르다 —
전부 진짜 데이터 손실인 텍스트와 달리, 일부는 "장식 vs 실제 콘텐츠"를 가려야 하는 경우가
있고, 이건 좌표만으로는 못 가른다.** 그래서 이미지 쪽 변경은 되돌렸고, 202508의 이미지
매칭률(81%/11%)은 이번 라운드에서 손대지 않음.

**8라운드에서 보류한 "비전 검증" 결정에 대한 참고**: 이번에 실제로 "좌표 규칙만으론 못
가르고 이미지 내용을 봐야 아는" 사례(202512의 장식 뱃지 vs 헤어스타일 사진)를 하나 발견함.
다만 이번엔 사람이 직접 이미지를 열어봐서 해결했고, 매 생성마다 자동으로 도는 상시 비전
검증 없이도 대응 가능했음 — 8라운드의 결론("실제로 필요성이 실증되는 사례가 나오면
재검토")을 뒤집을 만큼은 아니라고 판단해 보류 유지. 다만 완전히 무관한 얘기는 아니게 됐다는
점은 기록해둠 — 이미지 매칭 쪽에서 이런 충돌이 더 자주/심각하게 나오면 이 절부터 다시 볼 것.

### 10라운드 — icon_only가 다음 섹션의 진짜 이미지를 훔쳐가던 것 (실사용, 다운로드한 실제 pptx)
사용자가 웹 UI로 생성해서 다운로드한 실제 결과물(`gen_20260811_0449.pptx`, 202509 "할로윈
배틀패스")을 스크린샷 2장으로 신고 — "할로윈 호박 동작 선택 상자" 섹션에 나온 이미지 2장이
바로 다음 "할로윈 펌킨 버킷 등록권" 섹션에서 완전히 똑같이 한 번 더 나옴(이름은 다른데
사진이 두 곳에서 중복).

**근본 원인** (웹서비스 실제 업로드 파일 `web/backend/data/uploads/21.xlsx`로 재현·확인 —
`samples/202509_request.xlsx`와 동일 파일): "할로윈 호박 동작 선택 상자"는 항목 이름이 하나도
없는 진짜 `icon_only` 섹션이지만, 정작 자기 소유 이미지는 없음(제목 옆의 작은 뱃지 하나뿐,
7라운드 규칙으로 이미 제외됨). 바로 뒤에 이름표 없이 "CD50: 할로윈 펌킨 버킷 등록권 | 할로윈
잭 오 랜턴 등록권"처럼 항목명이 나오는 행이 있는데, 이건 AI가 별도의 `few_preview` 섹션으로
정확히 분류함(이미 few-shot 예시로 등록된 케이스). 문제는 이 두 실제 항목의 초상화 이미지가
행 36~50에 걸쳐 있다는 것 — icon_only가 계산한 자기 행 범위(34~49)와 "겹치기는" 하지만
안에 완전히 들어있지는 않음(50까지 삐져나옴). 9라운드까지의 `_icon_only_items` 필터는
"겹치기만 하면" 포함시켰어서, 이 두 이미지를 icon_only가 먼저 가로채고, 뒤이어
`few_preview` 섹션도 이름 기반 매칭으로 같은 두 이미지를 또 찾아내 — 결과적으로 같은 사진이
두 섹션에 각각 다른 캡션을 달고 중복 출력됨.

**수정**: `_icon_only_items()`의 이미지 선택 조건을 "겹치면 포함"에서 "완전히 범위 안에
들어와야 포함"(`start <= row_start` 그리고 `row_end <= end`)으로 강화. 자기 범위를 삐져나가는
이미지는 다른 섹션 소유로 보고 아예 손대지 않음.

**검증**: 202509 실제 파일로 재확인 — "할로윈 호박 동작 선택 상자"는 이제 진짜로 이미지가
0개라 렌더링 실패 처리되어 결과물에서 통째로 빠짐(원래도 자기 콘텐츠가 없었던 섹션이라
빈 페이지가 사라지는 게 맞는 결과), "할로윈 펌킨 버킷 등록권"의 2개 항목은 각각 다른
이미지로 정상 매칭(100%). 실제로 다시 렌더링해서 슬라이드에 같은 사진이 더 이상 두 번 안
나오는 것 PowerPoint로 직접 확인. 202602 기존 icon_only 회귀 케이스(이미지가 자기 행 범위
안에 완전히 들어있는 정상 케이스)는 여전히 통과 — 새 조건이 진짜 양성 케이스를 깨뜨리지
않음. 회귀 테스트 1개 추가, 144개 전체 통과.

### 기타
- 결과 파일명을 `gen_<날짜>_<시분>.pptx`로 변경 (기존 고정 `generated.pptx`에서)
- `SectionResult`에 `footnote`, `items` 필드 추가

## 테스트
143개 통과 (`pytest tests/`, CLI 파이프라인 기준 — 웹 백엔드는 별도 스위트). 실제
202503/202508/202512/202602/202605/202606 요청서를 활용한 회귀 테스트 다수 포함.

## 다음 단계
설계서 9단계(신규 월 실사용 테스트) — 다음 달 실제 request.xlsx가 오면 이번처럼 실제 산출물을
사용자가 확인하며 다듬는 라운드가 이어질 예정.
